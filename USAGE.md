# 🎮 迷你世界物品 ID 查询器 - 使用说明

## ✨ 功能特性

本插件提供**全方位查询**功能：
- 🔍 **按名称查 ID** - 输入物品名称，获取 ID（支持模糊搜索）
- 🔢 **按 ID 查名称** - 输入 ID，获取物品名称
- 📋 **浏览所有物品** - 分页查看完整物品清单
- ⚙️ **自定义显示数量** - 设置搜索结果默认显示条数
- 🎯 **高级批量搜索** - 一次显示多个搜索结果

---

## 📋 可用命令

### 1️⃣ 快速查询（`/mini_id`）

**用途**：根据物品名称搜索对应的 ID，适合快速查找

**使用方法**：
```
/mini_id <物品名称>
```

**示例**：
```
/mini_id 钻石
/mini_id 地心基石
/mini_id 南瓜
/mini_id 工具  # 模糊搜索
```

**预期回复**：
```
🔍 查询到 **5** 个相关物品:

• 钻石 - ID: `XXX` 【矿石】
• 钻石剑 - ID: `XXX` 【工具】
• 钻石镐 - ID: `XXX` 【工具】
• 钻石斧 - ID: `XXX` 【工具】
• 钻石铲 - ID: `XXX` 【工具】

💡 显示前 15 个结果，共 20 个匹配物品
```

**特点**：
- ✅ 最多显示 15 个结果
- ✅ 支持中文模糊搜索
- ✅ 智能排序，最匹配的排在前面

---

### 2️⃣ 高级搜索（`/mini_search`）⭐推荐

**用途**：高级搜索功能，可自定义显示数量，适合批量查找

**使用方法**：
```
/mini_search <物品名称> [显示数量]
```

**示例**：
```
# 使用默认数量（通过 /mini_setlimit 设置）
/mini_search 钻石

# 临时指定显示 100 个
/mini_search 工具 100

# 最多显示 500 个
/mini_search 方块 500
```

**预期回复**：
```
🔍 高级搜索：**工具**
找到 **150** 个相关物品 (显示前 100 个):

• 工具台 - ID: `201` 【工具】
• 工作台 - ID: `202` 【工具】
• 熔炉 - ID: `203` 【工具】
...

💡 还有 50 个结果未显示
📊 总共有 150 个匹配物品
```

**特点**：
- ✅ 默认显示数量可设置（通过 `/mini_setlimit`）
- ✅ 单次最多显示 500 个结果
- ✅ 支持模糊搜索和智能排序
- ✅ 清晰提示未显示的结果数量

---

### 3️⃣ 按 ID 查询（`/mini_search_id`）

**用途**：根据物品 ID 查找对应的物品名称

**使用方法**：
```
/mini_search_id <ID>
```

**示例**：
```
/mini_search_id 1
/mini_search_id 100
/mini_search_id 230
```

**预期回复**：
```
🔍 ID **1** 对应的物品:

• 地心基石 【方块】

💡 该 ID 对应 1 个物品
```

或者如果一个 ID 对应多个物品：
```
🔍 ID **XXX** 对应的物品:

• 物品 A 【类型】
• 物品 B 【类型】

💡 该 ID 对应 2 个物品（可能是不同版本或变体）
```

---

### 4️⃣ 设置显示数量（`/mini_setlimit`）

**用途**：设置 `/mini_search` 命令的默认显示数量

**使用方法**：
```
/mini_setlimit <数量>
```

**示例**：
```
# 设置为显示 200 个
/mini_setlimit 200

# 设置为显示 50 个
/mini_setlimit 50

# 查看当前设置
/mini_setlimit
```

**预期回复**：
```
✅ 搜索结果显示数量已设置为 **200** 个

💡 提示：
• 使用 `/mini_search` 时将默认显示 200 个结果
• 也可以在搜索时临时指定数量：`/mini_search 钻石 100`
```

**特点**：
- ✅ 一次设置，永久生效
- ✅ 可在搜索时临时覆盖
- ✅ 支持范围：1-500

---

### 5️⃣ 查看所有物品（`/mini_list`）

**用途**：浏览所有物品的 ID 列表，支持翻页

**使用方法**：
```
/mini_list [页码]
```

**示例**：
```
/mini_list          # 查看第 1 页
/mini_list 2        # 查看第 2 页
/mini_list 50       # 查看第 50 页
```

**预期回复**：
```
📋 **迷你世界物品 ID 列表** (第 1/100 页)

共计 2000 个物品 | 每页 20 条

• ID: `1` | 地心基石【方块】
• ID: `2` | 功能方块【方块】
• ID: `3` | 静态水【方块】
• ID: `4` | 水【方块】
• ID: `5` | 静态岩浆【方块】
...

━━━━━━━━━━━━━━━━━━
💡 使用方式:
• `/mini_list` - 查看第 1 页
• `/mini_list 2` - 查看第 2 页
• `/mini_list 100` - 查看最后一页

➡️ 下一页：`/mini_list 2`
```

---

### 6️⃣ 查看帮助（`/mini_help`）

**用途**：显示完整的使用指南

**使用方法**：
```
/mini_help
```

---

## 💡 使用提示

### 🔍 模糊搜索技巧

1. **部分匹配**：
   - ✅ `/mini_id 钻` → 会匹配"钻石"、"钻石剑"等
   - ✅ `/mini_id 基石` → 会匹配"地心基石"

2. **分词匹配**（新特性）：
   - ✅ `/mini_id 刚石` → 可以找到"金刚石"
   - ✅ `/mini_id 心基` → 可以找到"地心基石"
   - 即使字符顺序不同也能匹配

3. **二字词组匹配**：
   - ✅ `/mini_id 工台` → 可能找到"工作台"、"工具台"
   - 连续两个字符的组合会被识别

### ⚙️ 显示数量设置

1. **设置默认值**：
   ```
   /mini_setlimit 200  # 设置默认显示 200 个
   ```

2. **临时覆盖**：
   ```
   /mini_search 钻石 50  # 这次只显示 50 个
   ```

3. **推荐配置**：
   - 日常使用：50-100 个
   - 深度搜索：200-300 个
   - 批量导出：500 个（最大值）

### 📋 其他提示

1. **ID 必须是数字**：
   - ✅ `/mini_search_id 123` - 正确
   - ❌ `/mini_search_id abc` - 错误

2. **大小写不敏感**：
   - ✅ `/mini_id Diamond` 
   - ✅ `/mini_id diamond`
   - 效果相同

3. **翻页功能**：
   - `/mini_list` 每页显示 20 条
   - 支持 `/mini_list 2`、`/mini_list 10` 等

4. **一个 ID 可能对应多个物品**：
   - 某些 ID 可能对应多个变体或版本
   - 系统会列出所有匹配的物品

---

## 🌐 数据来源

- **数据源**：https://srcbs.cn/tool/mini/item.json
- **查询网站**： https://srcbs.cn/tool/mini/
- **数据格式**：JSON
- **更新频率**：插件启动时自动加载最新数据

---

## 🔧 技术细节

### 数据结构
```python
# 名称缓存：名称 → {id, type, full_name}
items_cache = {
    "地心基石": {
        "id": "1",
        "type": "方块",
        "full_name": "地心基石 (方块)"
    }
}

# ID 缓存：ID → [{name, type}]
id_to_name_cache = {
    "1": [
        {"name": "地心基石", "type": "方块"}
    ]
}
```

### 解析规则
- **格式**：`-ID-【类型】名称`
- **正则**：`r'-(\d+)-【(.*?)】(.*)'`
- **示例**：`-1-【方块】地心基石` → ID: `1`, 类型：`方块`, 名称：`地心基石`

---

## ❓ 常见问题

### Q: 为什么搜索不到某个物品？
A: 请检查：
1. 物品名称是否正确
2. 是否使用了完整的名称
3. 尝试使用部分名称进行模糊搜索

### Q: 为什么 ID 搜索返回多个结果？
A: 某些 ID 可能对应多个物品变体或不同版本的同名物品。

### Q: 数据是最新的吗？
A: 插件每次启动时都会从官网重新加载数据，确保数据最新。

---

## 🎯 快速参考

| 命令 | 用途 | 示例 |
|------|------|------|
| `/mini_id <名称>` | 快速查询 ID（最多 15 个） | `/mini_id 钻石` |
| `/mini_search <名称> [数量]` | 高级搜索（可指定数量） | `/mini_search 工具 100` |
| `/mini_search_id <ID>` | 按 ID 查名称 | `/mini_search_id 1` |
| `/mini_list [页码]` | 浏览所有物品（分页） | `/mini_list 2` |
| `/mini_setlimit <数量>` | 设置默认显示数量 | `/mini_setlimit 200` |
| `/mini_help` | 查看帮助 | `/mini_help` |

---

**开发者**：Ling_Yue  
**版本**：v1.1.0  
**数据源**：圣灵导航页 - 迷你世界物品 ID 查询
