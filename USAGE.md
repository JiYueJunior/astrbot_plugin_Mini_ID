# 🎮 迷你世界物品 ID 查询器 - 使用说明

## ✨ 功能特性

本插件提供**双向查询**功能：
- 🔍 **按名称查 ID** - 输入物品名称，获取 ID
- 🔢 **按 ID 查名称** - 输入 ID，获取物品名称

---

## 📋 可用命令

### 1️⃣ 按名称查询（`/mini_id`）

**用途**：根据物品名称搜索对应的 ID

**使用方法**：
```
/mini_id <物品名称>
```

**示例**：
```
/mini_id 钻石
/mini_id 地心基石
/mini_id 南瓜
```

**预期回复**：
```
🔍 查询到 **5** 个相关物品:

• 钻石 - ID: `XXX` 【矿石】
• 钻石剑 - ID: `XXX` 【工具】
• 钻石镐 - ID: `XXX` 【工具】
• 钻石斧 - ID: `XXX` 【工具】
• 钻石铲 - ID: `XXX` 【工具】
```

---

### 2️⃣ 按 ID 查询（`/mini_search_id`）

**用途**：根据物品 ID 查找对应的物品名称

**使用方法**：
```
/mini_search_id <ID>
```

**示例**：
```
/mini_search_id 1
/mini_search_id 100
/mini_search_id 230
```

**预期回复**：
```
🔍 ID **1** 对应的物品:

• 地心基石 【方块】

💡 该 ID 对应 1 个物品
```

或者如果一个 ID 对应多个物品：
```
🔍 ID **XXX** 对应的物品:

• 物品 A 【类型】
• 物品 B 【类型】

💡 该 ID 对应 2 个物品（可能是不同版本或变体）
```

---

### 3️⃣ 查看所有物品（`/mini_list`）

**用途**：浏览所有物品的 ID 列表，支持翻页

**使用方法**：
```
/mini_list [页码]
```

**示例**：
```
/mini_list          # 查看第 1 页
/mini_list 2        # 查看第 2 页
/mini_list 50       # 查看第 50 页
```

**预期回复**：
```
📋 **迷你世界物品 ID 列表** (第 1/100 页)

共计 2000 个物品 | 每页 20 条

• ID: `1` | 地心基石【方块】
• ID: `2` | 功能方块【方块】
• ID: `3` | 静态水【方块】
• ID: `4` | 水【方块】
• ID: `5` | 静态岩浆【方块】
...

━━━━━━━━━━━━━━━━━━
💡 使用方式:
• `/mini_list` - 查看第 1 页
• `/mini_list 2` - 查看第 2 页
• `/mini_list 100` - 查看最后一页

➡️ 下一页：`/mini_list 2`
```

---

### 4️⃣ 查看帮助（`/mini_help`）

**用途**：显示完整的使用指南

**使用方法**：
```
/mini_help
```

---

## 💡 使用提示

1. **模糊搜索**：支持部分名称匹配
   - ✅ `/mini_id 钻` → 会匹配"钻石"、"钻石剑"等
   
2. **ID 必须是数字**：
   - ✅ `/mini_search_id 123` - 正确
   - ❌ `/mini_search_id abc` - 错误

3. **大小写不敏感**：
   - ✅ `/mini_id Diamond` 
   - ✅ `/mini_id diamond`
   - 效果相同

4. **一个 ID 可能对应多个物品**：
   - 某些 ID 可能对应多个变体或版本
   - 系统会列出所有匹配的物品

---

## 🌐 数据来源

- **数据源**：https://srcbs.cn/tool/mini/item.json
- **查询网站**： https://srcbs.cn/tool/mini/
- **数据格式**：JSON
- **更新频率**：插件启动时自动加载最新数据

---

## 🔧 技术细节

### 数据结构
```python
# 名称缓存：名称 → {id, type, full_name}
items_cache = {
    "地心基石": {
        "id": "1",
        "type": "方块",
        "full_name": "地心基石 (方块)"
    }
}

# ID 缓存：ID → [{name, type}]
id_to_name_cache = {
    "1": [
        {"name": "地心基石", "type": "方块"}
    ]
}
```

### 解析规则
- **格式**：`-ID-【类型】名称`
- **正则**：`r'-(\d+)-【(.*?)】(.*)'`
- **示例**：`-1-【方块】地心基石` → ID: `1`, 类型：`方块`, 名称：`地心基石`

---

## ❓ 常见问题

### Q: 为什么搜索不到某个物品？
A: 请检查：
1. 物品名称是否正确
2. 是否使用了完整的名称
3. 尝试使用部分名称进行模糊搜索

### Q: 为什么 ID 搜索返回多个结果？
A: 某些 ID 可能对应多个物品变体或不同版本的同名物品。

### Q: 数据是最新的吗？
A: 插件每次启动时都会从官网重新加载数据，确保数据最新。

---

## 🎯 快速参考

| 命令 | 用途 | 示例 |
|------|------|------|
| `/mini_id <名称>` | 按名称查 ID | `/mini_id 钻石` |
| `/mini_search_id <ID>` | 按 ID 查名称 | `/mini_search_id 1` |
| `/mini_list [页码]` | 查看所有物品（分页） | `/mini_list 2` |
| `/mini_help` | 查看帮助 | `/mini_help` |

---

**开发者**：Ling_Yue  
**版本**：v1.0.0  
**数据源**：圣灵导航页 - 迷你世界物品 ID 查询
